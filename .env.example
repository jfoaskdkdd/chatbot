# Example environment variables for deployment
# Name of the wppconnect session (folder under tokens will be created)
WPP_SESSION=disparador

# Puppeteer/Chrome settings
# If the Docker image doesn't include Chrome, point to a system-installed binary
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
PUPPETEER_DUMPIO=0

# Bot behavior
MAX_OPA=300
LAST_N=8
DEFAULT_COUNTRY_CODE=55
INDEX_REFRESH_INTERVAL_MS=120000

# Debugging
WPP_DEBUG_MATCH=0
